groups:
  - name: demo-alerts
    rules:
      - alert: DemoAlwaysFiring
        expr: vector(1)
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "Demo alert firing"
          description: "This alert always fires to test Alertmanager."
